# LinShare MCP Server Configuration Template

# --- LinShare API URLs ---
# User API (JWT based)
LINSHARE_USER_URL=https://your-instance.com/linshare/webservice/rest/user/v5

# Delegation API (Basic Auth based)
# Note: Use v2 for modern administrative tools
LINSHARE_ADMIN_URL=https://your-instance.com/linshare/webservice/rest/delegation/v2

# --- Authentication ---
# Admin Credentials (for delegation tools)
LINSHARE_USERNAME=your-service-account@example.com
LINSHARE_PASSWORD=your-secret-password

# User JWT (Optional, for persistent session)
# LINSHARE_JWT_TOKEN=your-pre-generated-jwt-here

# --- Storage & Directories ---
# Directory for temporary file assembly and pick-up
LINSHARE_UPLOAD_DIR=./LinShareUploads
# Directory where files downloaded via tools are stored
LINSHARE_DOWNLOAD_DIR=./LinShareDownloads

# --- Server Settings ---
# Default mode: all, admin, or user
LINSHARE_MCP_MODE=all
# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
