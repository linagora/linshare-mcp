# LinShare MCP Server Configuration Template

# --- LinShare API URLs ---
# User API (JWT based)
LINSHARE_USER_URL=https://your-instance.com/linshare/webservice/rest/user/v5

# Delegation API (Basic Auth based)
# Note: Use v2 for modern administrative tools
LINSHARE_ADMIN_URL=https://your-instance.com/linshare/webservice/rest/delegation/v2

# --- Authentication ---
# Admin Credentials (for delegation tools)
LINSHARE_USERNAME=your-service-account@example.com
LINSHARE_PASSWORD=your-secret-password

# User JWT (Optional - Recommended to invoke via Chat UI)
# LINSHARE_JWT_TOKEN=your-jwt-token

# --- Server Settings ---
# Default mode: all, admin, or user
LINSHARE_MCP_MODE=all
# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# --- MPC Server Address (for Chat Client) ---
MCP_SERVER_SSE_URL=http://localhost:8000/sse

# --- LLM Configuration ---
# Options: google, deepseek, groq, local
LLM_PROVIDER=google

# 1. Google Gemini (Default)
GOOGLE_API_KEY=your_gemini_key

# 2. DeepSeek
# DEEPSEEK_API_KEY=your_deepseek_key

# 3. Groq (Llama 3)
# GROQ_API_KEY=your_groq_key

# 4. Local LLM (OpenAI Compatible)
# LOCAL_LLM_URL=http://localhost:1234/v1
# LOCAL_LLM_MODEL=local-model
# LOCAL_LLM_API_KEY=not-needed

# --- Voice Transcription (Optional) ---
# Required for voice prompts (uses Groq Whisper)
TRANSCRIPTION_PROVIDER=groq
GROQ_API_KEY=your_groq_key  # Required for voice features
